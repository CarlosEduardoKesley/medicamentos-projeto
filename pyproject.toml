[project]
name = "medicamentos-app"
version = "1.0.0"
description = "Controle de medicamentos para cuidadores com interface web e alertas climáticos."
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx",
    "jinja2",
    "python-multipart",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "ruff>=0.1.0",
    "httpx",
]

services:
  - type: web
    name: medcuidar
    runtime: python
    buildCommand: pip install -e ".[dev]" && pip install python-multipart pytest-asyncio
    startCommand: uvicorn src.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0